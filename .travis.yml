# set language
language: java

# temporary change permissions on gradle wrapper
before_install:
 - cd open.xtrace
 - chmod +x gradlew
 - cd ..
 - cd open.xtrace.adapters
 - chmod +x gradlew
 - cd ..

jdk:
  - oraclejdk8

# run gradle
script:
  - ./gradlew
